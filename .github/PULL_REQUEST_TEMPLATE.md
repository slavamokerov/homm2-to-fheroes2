## What does this PR do?

<!-- A short summary of the change and why it is needed. -->

## Related issues

<!-- e.g. Fixes #123 -->

## Checklist

- [ ] The code follows the style of the surrounding files
      (C++17, English comments, `h2` / `fh2` namespaces).
- [ ] `cmake --build build && ctest --test-dir build` passes.
- [ ] For web changes: `./web/build.sh` builds and the page was
      checked manually when possible.
- [ ] Documentation (`README.md`, `docs/*`) is updated where the
      behavior or the format changed.
- [ ] No unrelated changes are included.
- [ ] Fixture saves from the original game have not been added
      (they are not redistributed in the repository).
